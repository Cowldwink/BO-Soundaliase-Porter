@{
    PanType = @(
        "2d"
        "3d"
    )

    Storage = @(
        "unknown"
        "loaded"
        "streamed"
        "primed"
    )

    LimitType = @(
        "none"
        "oldest"
        "reject"
        "priority"
    )

    Looping = @(
        "nonlooping"
        "looping"
    )

    RandomizeType = @(
        "volume"
        "pitch"
        "variant"
    )

    Boolean = @(
        "yes"
        "no"
    )
}
